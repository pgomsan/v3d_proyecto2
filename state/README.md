# State

Carpeta reservada para persistencia de la aplicacion.

Los archivos JSON pueden editarse a mano durante el desarrollo. Los ficheros `.jsonl` de `logs/` estan pensados para guardar una entrada por linea con timestamp.
